version: "3.9"

services:
  # ── Infrastructure ────────────────────────────────────────────────
  grobid:
    image: lfoppiano/grobid:0.8.1
    ports:
      - "8070:8070"
    networks:
      - episteme

  neo4j:
    image: neo4j:5-community
    environment:
      NEO4J_AUTH: neo4j/episteme
    volumes:
      - neo4j_data:/data
    networks:
      - episteme

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio_data:/data
    networks:
      - episteme

  # ── Application ───────────────────────────────────────────────────
  api-server:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      JWT_SECRET: ${JWT_SECRET:-episteme-dev-secret-change-in-prod}
      LLM_PROVIDER: ${LLM_PROVIDER:-openai}
      LLM_API_KEY: ${LLM_API_KEY:-${OPENAI_API_KEY}}
      LLM_FAST_MODEL: ${LLM_FAST_MODEL:-${OPENAI_FAST_MODEL:-o3-mini}}
      LLM_STANDARD_MODEL: ${LLM_STANDARD_MODEL:-${LLM_ANALYSIS_MODEL:-${OPENAI_ANALYSIS_MODEL:-o3-mini}}}
      LLM_DEEP_MODEL: ${LLM_DEEP_MODEL:-${LLM_STANDARD_MODEL:-${LLM_ANALYSIS_MODEL:-${OPENAI_ANALYSIS_MODEL:-o3-mini}}}}
      LLM_ANALYSIS_MODEL: ${LLM_ANALYSIS_MODEL:-${OPENAI_ANALYSIS_MODEL:-o3-mini}}
      LLM_EMBEDDING_MODEL: ${LLM_EMBEDDING_MODEL:-${OPENAI_EMBEDDING_MODEL:-text-embedding-3-large}}
      LLM_EMBEDDING_DIM: ${LLM_EMBEDDING_DIM:-3072}
      # Vertex AI ADC 設定 (LLM_PROVIDER=google)
      GCP_PROJECT_ID: ${GCP_PROJECT_ID:-${GOOGLE_CLOUD_PROJECT:-}}
      GCP_LOCATION: ${GCP_LOCATION:-${GOOGLE_CLOUD_LOCATION:-us-central1}}
      GCP_USE_VERTEX_AI: ${GCP_USE_VERTEX_AI:-true}
      # 認証ファイルパス: ホストの .gcp/ をコンテナ内 /app/.gcp/ にマウントした場合の固定パス。
      # .env で上書き可能 (例: GOOGLE_APPLICATION_CREDENTIALS=/app/.gcp/sa.json)
      GOOGLE_APPLICATION_CREDENTIALS: ${GOOGLE_APPLICATION_CREDENTIALS:-/app/.gcp/application_default_credentials.json}
      # Vertex AI ADC 設定 (LLM_PROVIDER=gemini-vertex のときのみ有効、廃止予定)
      GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT:-}
      GOOGLE_CLOUD_LOCATION: ${GOOGLE_CLOUD_LOCATION:-us-central1}
      NEO4J_URI: ${NEO4J_URI}
      NEO4J_AUTH: ${NEO4J_AUTH}
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT}
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-${MINIO_ROOT_USER}}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-${MINIO_ROOT_PASSWORD}}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
      # Domain cartridge
      EPISTEME_DEFAULT_CARTRIDGE_ID: ${EPISTEME_DEFAULT_CARTRIDGE_ID:-particle_physics}
      EPISTEME_CARTRIDGES_DIR: ${EPISTEME_CARTRIDGES_DIR:-}
      GROBID_URL: ${GROBID_URL:-http://grobid:8070}
      # 音声文字起こし（ハンズフリー会話、openai プロバイダのみ）
      LLM_TRANSCRIBE_MODEL: ${LLM_TRANSCRIBE_MODEL:-whisper-1}
      # TensionMiningAgent (B層) — コスト制御
      TENSION_MAX_CALLS_PER_SESSION: ${TENSION_MAX_CALLS_PER_SESSION:-3}
      TENSION_MAX_CALLS_PER_DAY: ${TENSION_MAX_CALLS_PER_DAY:-10}
      TENSION_LLM_MODEL: ${TENSION_LLM_MODEL:-}
    # ローカルの .gcp/ をコンテナに読み取り専用でマウントする。
    # ファイルが存在しない場合は空ディレクトリとしてマウントされる（エラーにはならない）。
    # サービスアカウントキーを使う場合: .gcp/credentials.json に配置する。
    # gcloud ADC を使う場合: .gcp/application_default_credentials.json に配置し
    #   GOOGLE_APPLICATION_CREDENTIALS=/app/.gcp/application_default_credentials.json を .env に記載。
    volumes:
      - ./.gcp:/app/.gcp:ro
    depends_on:
      postgres:
        condition: service_healthy
      neo4j:
        condition: service_started
      minio:
        condition: service_started
      grobid:
        condition: service_started
    networks:
      - episteme
    extra_hosts:
      - "host.docker.internal:host-gateway"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - api-server
    networks:
      - episteme

volumes:
  neo4j_data:
  postgres_data:
  minio_data:

networks:
  episteme:
    driver: bridge
