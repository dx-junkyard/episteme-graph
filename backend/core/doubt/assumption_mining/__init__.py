"""暗黙前提マイニング（D2-2 経路A / D3-5 経路B）。

経路A（mined_gap）: AIが行間を埋めるために補った補完ステップ（inferred /
review_required / fallback）を、複数論文・複数導出にまたがる反復として検出し、
暗黙前提の **候補** に昇格させる。検出は非LLM（detector.py）、
「原子化された前提文」への正規化のみ LLM（非同期 worker）。

経路B（mined_corpus）: 「ほぼすべての導出が依存しているのに、どの論文も
それ自体を主張・引用・防御していないノード」= 引用グラフの空白を検出する
（corpus_audit.py, 非LLM）。

不変条項:
  - 出力は常に status='candidate'。confirmed への遷移は教員 API（D2-4）のみ。
  - LLM 正規化に 2 回失敗しても検出クラスタは行として保持（P4）。
  - 単一導出にしか現れない補完は候補化しない（2 論文以上 or 2 導出以上）。
  - 3 論文未満のコーパスでは経路B を実行しない（小規模コーパスの偽陽性防止）。
"""
